# SQLite for local dev (file path relative to prisma/)
DATABASE_URL="file:./dev.db"

# Auth (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET="change-me-to-a-long-random-string"
NEXTAUTH_URL="http://localhost:3000"
